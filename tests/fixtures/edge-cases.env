# Edge cases
EMPTY_VALUE=
DOUBLE_QUOTED="hello world"
SINGLE_QUOTED='hello world'
EQUALS_IN_VALUE=key=value=extra
export EXPORTED_KEY=secret
MULTILINE_KEY=line one \
  line two \
  line three
AFTER_MULTILINE=normalvalue

# Duplicate key test
DUPLICATE_KEY=first
DUPLICATE_KEY=second
